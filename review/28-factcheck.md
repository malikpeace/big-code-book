# Chapter 28 fact-check

## Direct fixes applied

None.

## Findings

1. **WRONG:** The chapter twice calls `memento-app/js/33-action-shim.js` a 38-line shim. It is exactly 37 lines at web pin `ca8afc8`, as chapters 1 and 26 correctly state.
2. **NIT:** The recovery exercise says `git revert <hash>` opens an editor. That depends on the user's Git editor configuration and can be avoided for a beginner with `git revert --no-edit <hash>`.

## Verification

- The paywall delegation, stale decision record, three production inline `onclick` attributes versus 1,112 JS-file `addEventListener` occurrences, emergency switch, migration protocol, and ownership rule match the pins.
- The deliberate `git reset --hard` occurs only inside the disposable practice repo and is paired with reflog recovery.
- No em dashes found in the chapter source.
