# Git lab

Working directory: `~/Downloads/CODE-BOOK/git-lab`. Expected output: three commits, two branches, and one intentional merge conflict in `app.js`. Reset command: `./make-git-lab.sh`. This cannot touch Memento.

The setup script refuses to reset any folder except this exact lab path. It creates a fresh nested Git repository, so every commit, branch, merge, and reset stays disposable.
