# Hints

1. Start at `checkAuth(req)`.
2. The function checks an owner name, but what proves the caller passed the front desk?
3. Compare `checkAuth` with `../../v1-secure/server.js` and restore the `fake_token` test.
