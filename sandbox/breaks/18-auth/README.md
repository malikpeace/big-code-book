# Chapter 18 break: identity check removed

Working directory: `~/Downloads/CODE-BOOK/sandbox/breaks/18-auth`. Expected output: a caller with an owner header but no token can read that owner's data. Reset command: copy `server.js` from `../../v1-secure/`. This cannot touch Memento.
