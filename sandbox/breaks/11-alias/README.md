# Chapter 11 break: shared reference

Working directory: `~/Downloads/CODE-BOOK/sandbox/breaks/11-alias`. Expected output: resetting lets later changes mutate the object that was supposed to remain the default. Reset command: copy `state.js` from `../../v1/`. This cannot touch Memento.
