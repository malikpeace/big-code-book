# Hints

1. Look only at `resetState()`.
2. Ask whether `state` receives a new object or a second label for an old object.
3. Compare `state = DEFAULT_STATE` with the reset line in `../../v1/state.js`.
