# Hints

1. Open the browser console and reload the page.
2. A 404 means the browser walked to an address where no file exists.
3. Compare the two script names at the bottom of `index.html` with the files from `ls`.
