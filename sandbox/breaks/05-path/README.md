# Chapter 5 break: wrong path

Working directory: `~/Downloads/CODE-BOOK/sandbox/breaks/05-path`. Expected output: the page loads but its controls do not work, and the browser reports one missing file. Reset command: copy `index.html` from `../../v1/`. This cannot touch Memento.
