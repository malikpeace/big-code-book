# Chapter 23 break: runtime

Working directory: `~/Downloads/CODE-BOOK/sandbox/breaks/23-runtime`. Expected output: both files pass `node --check`, then the page throws while booting. Reset command: copy the four app files from `../../v1/`. This cannot touch Memento.
