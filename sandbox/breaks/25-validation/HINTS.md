# Hints

1. The browser's `maxlength` protects a typing interface, not the server.
2. Read every rule in `validateState()` and look for a type check with no size check.
3. Compare the function with `../../v1-secure/server.js`.
