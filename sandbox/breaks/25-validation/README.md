# Chapter 25 break: missing input limit

Working directory: `~/Downloads/CODE-BOOK/sandbox/breaks/25-validation`. Expected output: the server accepts a north star longer than the app's 200-character limit. Reset command: copy `server.js` from `../../v1-secure/`. This cannot touch Memento.
