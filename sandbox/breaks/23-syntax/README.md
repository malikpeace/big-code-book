# Chapter 23 break: syntax

Working directory: `~/Downloads/CODE-BOOK/sandbox/breaks/23-syntax`. Expected output: `node --check state.js` reports a syntax error. Reset command: copy the four app files from `../../v1/`. This cannot touch Memento.
