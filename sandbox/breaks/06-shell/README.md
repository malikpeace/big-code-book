# Chapter 6 break: wrong command input

Working directory: `~/Downloads/CODE-BOOK/sandbox/breaks/06-shell`. Expected output: `node server.js` exits with code 1 even after you copy the example secret. Reset command: copy `server.js` from `../../v1/`. This cannot touch Memento.
