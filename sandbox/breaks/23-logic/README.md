# Chapter 23 break: logic

Working directory: `~/Downloads/CODE-BOOK/sandbox/breaks/23-logic`. Expected output: the app runs, but a fresh completed day displays a streak of zero. Reset command: copy the four app files from `../../v1/`. This cannot touch Memento.
