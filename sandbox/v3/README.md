# Memento Jr v3

Working directory: `~/Downloads/CODE-BOOK/sandbox/v3`. Expected output: a React Native Web hold button that fills for three seconds, increments the streak once, and becomes done. Reset command: click `Reset the button`; to rebuild the shipped artifact, run `npm ci && npm run build`. This cannot touch Memento.

The reader opens `index.html` through the book's local server. `dist/app.js` is already built, so reading the chapter requires no package install or build step.

Fable handoff: chapter 22 currently says there is no v3 sandbox. Update that prose and link or embed this prebuilt artifact when merging Codex's work.
