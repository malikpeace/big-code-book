# CODEX: what to do when your usage is back

From Fable, 2026-09-20 (night). Malik relays. Read this whole file, then CODEX-BRIEF.md Jobs 6, 7, 3 (the CODEX-marked parts), and 5, in that order.

## What exists now
- The site: `index.html`, `glossary.html`, `glossary.json`, `css/book.css`, `js/book.js`, `chapters/00-template.html`. Plain HTML/CSS/JS, no build. Passphrase gate (constant in book.js). Sandbox runner (Web Worker + 3s kill; iframe for `data-dom` boxes). Device gating (`data-device="mac"`). Glossary chips (`<dfn data-term>`). Progress in localStorage.
- Chapters written by Fable: see `ls chapters/`. Each follows CHAPTER-GUIDE.md.
- `glossary-additions/*.json`: per-chapter term files the writers produced; Fable merges them into glossary.json. If any are still unmerged when you arrive, merge them (same shape, later `ch` levels appended, no duplicates) and delete the additions file.
- Memento Jr: `sandbox/v1` (plain JS + intentionally incomplete server), `sandbox/v1-secure` (the evolved server, tagged CHAPTER 17/18/25), `sandbox/v2` (TypeScript + six planted type errors in `v2/errors/`), `sandbox/README.md`.
- Pins: `research/MEMENTO-PIN` (web ca8afc8, native 93bfa796).
- Serve locally: `cd ~/Downloads/CODE-BOOK && python3 -m http.server 8477` then open http://127.0.0.1:8477/index.html, passphrase in book.js.

## Your jobs, in order
1. **Fact-check every chapter that exists** (CODEX-BRIEF Job 6). One file per chapter: `review/NN-factcheck.md`, numbered findings, tagged WRONG / MISLEADING / MISSING / NIT. Re-verify every `path:lines` against the pins with `git -C ~/Downloads/MEMENTO show <pin>:<path> | sed -n 'a,bp'`. Check every date in chapter 4 against `research/research-ai-timeline.md` and its URLs. Run every sandbox mentally or in a browser; confirm the planted bugs and the "expected output" lines are true. Quote any sentence a beginner would misread. Grep every chapter for em dashes (must be zero) and for `<!-- CODEX: verify -->` markers the writers left; resolve each one.
2. **Do NOT edit chapter prose.** Put fixes in the review file. Exception: a wrong line number in a `.src` line or a wrong date. Those you may fix directly, and list what you changed at the top of the review file.
3. **Citation inventory** (Job 7): `research/CITATIONS.md` listing chapter, path, lines, pin, and the sha256 of the cited range, generated by a script you commit as `tools/cite-check.sh` so it can be re-run when Memento moves.
4. **Build `git-lab/`** (Job 3, CODEX): `make-git-lab.sh` creates a disposable repo with three commits, two branches, one staged conflict; re-running resets it. Chapters 7-8 already point at `~/Downloads/CODE-BOOK/git-lab`.
5. **Build `lab/`** (Job 3, CODEX): `make-native-lab.sh` exports the native pin into `lab/memento-native/` via `git -C ~/Downloads/MEMENTO archive 93bfa796 memento-native | tar -x -C lab/`. Gitignored. Chapters 24 and 29 will use it. Verify `npm ci && npm run check` passes there (this is the ONLY place npm runs; never inside MEMENTO).
6. **Build `breaks/`** (Job 3, CODEX): one folder per planted-bug exercise; read each chapter's "Do it" section for the bug it describes. `HINTS.md` (three hints) + `FIX.md` per folder.
7. **Interactives** (Job 5) in priority order, each a drop-in `<script>` + one `<div>`: journey-map explorer, event-loop visualizer, request inspector, git graph playground, diff-reading trainer, stack-trace reader, SQL policy simulator (labelled a simulator).
8. **Shell review** (`review/shell-review.md`): iOS Safari behaviour of the sandbox runner, contrast in both themes, anything broken at 390 or 1440, accessibility of the `<dfn>` popovers and `<details>` checks.
9. **Raster art** only where a photo-like image helps (chapter 3 history, chapter 4 timeline). SVG stays Fable's.

## Rules that do not move
- `~/Downloads/MEMENTO` is read-only. Reading, grepping, `git show <pin>:<path>`, `git log` are fine. Never write, commit, install, checkout, stash there.
- Commit only your own files in CODE-BOOK. Never `git add -A`.
- No em dashes anywhere. Dark-first, sharp, no gold, no serifs, no emoji icons.
- Nothing ships with an open WRONG or MISLEADING finding. Fable fixes prose; you re-check.
